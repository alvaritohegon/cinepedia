<div id="my-reviews-container">
  <h2 class="reviews-title">Reseñas</h2>

  {{#if mappedReviews}}

    {{#each mappedReviews}}
      <div class="my-review-card">
        <!-- Imagen de la película -->
        <img
          src="https://image.tmdb.org/t/p/w500{{this.moviePoster}}"
          alt="{{this.movieTitle}}"
          class="review-movie-poster"
        />

        <div class="review-info">
          <h3 class="review-movie-title">{{this.movieTitle}}</h3>
          <p class="review-rating">⭐ {{this.rating}} / 5</p>
          <p class="review-comment">{{this.comment}}</p>

          <div class="review-buttons">
            <form action="/reviews/{{this._id}}/delete" method="post">
              <button class="btn btn-danger">Borrar</button>
            </form>
          </div>
        </div>
      </div>
    {{/each}}

  {{else}}
    <p style="color:red">No hay ninguna reseña para este usuario</p>
  {{/if}}
</div>